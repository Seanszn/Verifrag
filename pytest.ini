[pytest]
testpaths = tests
addopts = --basetemp=.pytest_tmp
markers =
    smoke: fast, high-signal regression checks for core repo functionality
    integration: opt-in tests that hit live external processes or running services
